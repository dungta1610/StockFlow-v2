# Base URL of the StockFlow API as the browser reaches it. Its origin must be listed in the
# API's CORS_ORIGINS, and both must be on the same site (e.g. both on localhost) for the
# SameSite=Strict refresh cookie to be sent.
VITE_API_URL=http://localhost:3100
