server {
  listen 80;
  root /usr/share/nginx/html;

  # Client-side routing: unknown paths serve the SPA shell.
  location / {
    try_files $uri /index.html;
  }

  location = /healthz {
    access_log off;
    return 200 "ok";
  }
}
